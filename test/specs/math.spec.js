describe('Math Helpers', () => {
	describe('Constrain Function', () => {
		const range = [10, 100];

		xit('should return value if within range.');
		xit('should return min if value is less than range.');
		xit('should return max if value is greater than range.');
	});
});